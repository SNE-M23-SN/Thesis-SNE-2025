---
galaxy_info:
  author: Khasan Abdurakhmanov
  description: Ansible role for deploying SAST (Static Application Security Testing) platform with Docker TLS
  company: DevSecOps Platform
  license: MIT
  min_ansible_version: 2.9
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
  galaxy_tags:
    - docker
    - sast
    - security
    - tls
    - certificates
    - static-analysis
    - devsecops
    - semgrep
    - trivy
    - bearer
    - horusec

#dependencies:
  # Core infrastructure dependencies
#  - role: common
  
  # SAST role requires certificates generated by FirstStage
  # Note: FirstStage should be deployed before SAST to generate certificates
#  - role: firststage

collections:
  - community.docker
  - ansible.posix

